meta:
  environment: nodejs
  toolVersion: "20"

build:
  buildDir: .
  buildCommand: rm -rf node_modules && npm install --omit=dev

run:
  persistenceMount: /data
  runCommand: node src/index.js
  containerPort: 3000
