meta:
  environment: nodejs
  toolVersion: "20"

build:
  dockerfile: false
  buildDir: backend
  buildCommand: cd backend && npm install --production

run:
  persistenceMount: /data
  runCommand: node backend/src/index.js
  containerPort: 3000